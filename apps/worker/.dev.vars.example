# Local development secret only. Production secret should be set with:
# pnpm dlx wrangler secret put IDENTITY_SIGNING_SECRET --config apps/worker/wrangler.jsonc
IDENTITY_SIGNING_SECRET=replace-with-a-long-random-secret
# Optional local-only experiment flags:
# CURSOR_PULL_FIRST_POST_SCOPE_BYPASS=0
