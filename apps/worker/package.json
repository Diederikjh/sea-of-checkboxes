{
  "name": "@sea/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'wrangler dev not configured yet; local integration uses Vitest'",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured for @sea/worker'"
  },
  "dependencies": {
    "@sea/domain": "workspace:*",
    "@sea/protocol": "workspace:*"
  }
}
