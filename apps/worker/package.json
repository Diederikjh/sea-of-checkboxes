{
  "name": "@sea/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm dlx wrangler dev --local --config wrangler.jsonc --port 8787",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured for @sea/worker'"
  },
  "dependencies": {
    "@sea/domain": "workspace:*",
    "@sea/protocol": "workspace:*"
  }
}
